{
  "name": "nfcking-admin",
  "version": "1.0.0",
  "private": true,
  "description": "NFCKING adminpanel og Netlify functions for kortproduksjon, opplasting og invitasjoner",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site – ingen build step' && exit 0",
    "serve": "netlify dev",
    "lint": "eslint . --ext .js",
    "start": "netlify dev"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "stripe": "^14.0.0",
    "nodemailer": "^6.9.14"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "netlify-cli": "^17.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "netlify": {
    "build": {
      "functions": "netlify/functions",
      "publish": "."
    },
    "redirects": [
      {
        "from": "/api/*",
        "to": "/.netlify/functions/:splat",
        "status": 200
      }
    ]
  },
  "keywords": [
    "nfcking",
    "nfc",
    "firebase",
    "netlify",
    "wayback",
    "admin",
    "kortproduksjon"
  ]
}
